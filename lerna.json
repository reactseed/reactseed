{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "version": "independent",
  "useWorkspaces": true,
  "ignoreChanges": ["ignored-file", "**/__tests__/**", "**/*.md","**/*.yml"],
  "command": {
    "allowBranch": "main",
    "publish": {
      "verifyAccess": false,
      "registry": "http://9.134.97.81:8080/"
    },
    "version": {
      "allowBranch": ["main","dev"],
      "conventionalCommits": true
    }
  }
}
