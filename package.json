{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna clean -y",
    "bootstrap": "lerna bootstrap",
    "version": "lerna version",
    "publish": "lerna publish",
    "ls": "lerna list"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}